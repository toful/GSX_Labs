#!/bin/sh
# Start/stop the backup service.
#
### BEGIN INIT INFO
# Provides:          backup
# Required-Start:    $remote_fs $syslog $time
# Required-Stop:     $remote_fs $syslog $time
# Should-Start:      $network $named slapd autofs ypbind nscd nslcd winbind
# Should-Stop:       $network $named slapd autofs ypbind nscd nslcd winbind
# Default-Start:
# Default-Stop:		 3
# Short-Description: Backup service program
# Description:       backup is a program that runs when the machine is shutted
#					 down creating a compressed system backup for every user in
#					 the system.
### END INIT INFO

PATH=/bin:/usr/bin:/sbin:/usr/sbin
DESC="backup service"
NAME=backup
SCRIPTNAME=/etc/init.d/"$NAME"

for user in $(/home/)
do
	#comprovem que no hi hagi una backup del mateix dia de l'usuari
	if [ -e /back/$user/$(date +%y%m%d) ]
	then
		#comprovem si hi ha un backup d'un dia anterior

			#date +%y%m%d 

done
